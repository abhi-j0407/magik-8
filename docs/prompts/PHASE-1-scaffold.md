# Agent Prompt — PHASE-1 Scaffold

**Role:** Implementer (Scaffold)  
**Do not read:** full PRD, ANSWERS copy tables  
**Read:** `docs/HANDOFF.md`, `docs/ARCHITECTURE.md`, `docs/PRD.md` (§ REQ-033, REQ-060 setup only)

## Task

Scaffold Magik 8: Vite + React 19 + TypeScript + Tailwind v4 + vite-plugin-pwa + Vitest + basic-ssl dev script.

## Deliverables

- `package.json`, `vite.config.ts`, `index.html`, `src/main.tsx`, `src/App.tsx` (placeholder)
- `src/index.css` with DESIGN color tokens
- Empty dirs per ARCHITECTURE file tree
- `README.md` root with dev instructions (`npm run dev:https`)
- `npm run build` && `npm test` pass (empty test ok)

## Out of scope

No ball UI, no shake, no answers data yet.

## Handoff

Emit Handoff block; Coordinator updates `HANDOFF.md` to PHASE-2 ready.
